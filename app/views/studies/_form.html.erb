<%= simple_form_for(@study) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :study_code %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
